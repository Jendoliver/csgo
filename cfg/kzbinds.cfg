exec unbinds
unbind 1
unbind 2
unbind 3
unbind 4

bind mwheelup +jump
bind mwheeldown +jump
bind space +jump
bind mouse2 +duck
bind mouse4 "say !checkpoint"
bind mouse5 +voicerecord
bind f "say !flashlight"
bind l "say !ljblock"
bind p "say !profile"
bind o "say !options"
bind m "say !menu"
bind h "say !hide"
